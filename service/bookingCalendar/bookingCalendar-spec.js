describe('bookingCalendar', function() {

  beforeEach(module('chata-v-krpacove'));

  it('should ...', inject(function(bookingCalendar) {

	//expect(brewCalendar.doSomething()).toEqual('something');

  }));

});
